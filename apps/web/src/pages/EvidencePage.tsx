import requirementsDocument from "../generated/requirements.json";
import { Badge, PageHeader, Panel } from "../components/Ui";
import { statusLabel } from "../lib/domain";
import { Localized } from "../i18n/LanguageProvider";

const toneForStatus = (status: string) => status === "VERIFIED" ? "cyan" : status === "BLOCKED" ? "rose" : status === "IMPLEMENTING" ? "amber" : "neutral";

export function EvidencePage() {
  const requirements = requirementsDocument.requirements;
  const verified = requirements.filter((item) => item.status === "VERIFIED").length;
  return <Localized><><PageHeader eyebrow="DELIVERY EVIDENCE" title="Proof before progress" description="Architecture explains intent. Only reproducible tests and external readback can advance a requirement to verified." actions={<Badge tone={verified ? "cyan" : "neutral"}>{verified} / 15 VERIFIED</Badge>} />
    <div className="evidence-summary"><Panel><span>Requirements</span><strong>15</strong><small>Frozen coursework scope</small></Panel><Panel><span>Externally verified</span><strong>{verified}</strong><small>Derived from the YAML ledger</small></Panel><Panel><span>Current public state</span><strong>{verified === 15 ? "Complete" : "In progress"}</strong><small>No UI-only completion claims</small></Panel></div>
    <section className="section-block"><div className="section-heading"><span className="eyebrow">ARCHITECTURE</span><h2>Hybrid execution, explicit authority</h2><p>Cloudflare serves the experience; AWS runs transaction and asynchronous workloads; Sepolia is authoritative for asset and settlement state.</p></div><Panel className="diagram-panel"><img src="/architecture/system-context.svg" alt="Agent Market system architecture showing Cloudflare, AWS, PostgreSQL, queues, ECS, and Sepolia" /><a className="text-link" href="https://github.com/Tiancheng-Xu/agent-market/blob/main/docs/architecture/agent-market-system-context.mmd">View Mermaid source</a></Panel></section>
    <section className="section-block"><div className="section-heading"><span className="eyebrow">REQUEST SEQUENCE</span><h2>One request ID, every boundary</h2><p>State and outbox commit together; consumers are idempotent; chain completion is accepted only after receipt and event verification.</p></div><Panel className="diagram-panel"><img src="/architecture/request-sequence.svg" alt="Request lifecycle sequence across browser, edge, API, database, queues, matcher, and Sepolia" /><a className="text-link" href="https://github.com/Tiancheng-Xu/agent-market/blob/main/docs/architecture/agent-market-request-sequence.mmd">View Mermaid source</a></Panel></section>
    <section className="section-block"><div className="section-heading"><span className="eyebrow">PERFORMANCE DEGRADATION</span><h2>Keep the app usable without faking success</h2><p>Agent Market follows the Web3 coursework evidence standard as a checklist, but the implementation stays project-specific: SSR first, hydrate safely, lazy-load heavy Evidence media, surface offline/degraded health, and record only sanitized reason codes.</p></div><div className="evidence-gallery"><Panel className="evidence-shot"><img src="/architecture/performance-degradation.svg" alt="Agent Market performance degradation architecture" loading="lazy" /><h3>Degradation architecture</h3><p>Browser signals, Edge SSR, React degraded mode, health/API timeouts, local runtime, provider APIs, and Evidence records are separated so one slow dependency cannot collapse the page.</p><a className="text-link" href="https://github.com/Tiancheng-Xu/agent-market/blob/main/docs/architecture/agent-market-performance-degradation.mmd">View Mermaid source</a></Panel><Panel className="evidence-shot"><img src="/architecture/performance-degradation-sequence.svg" alt="Agent Market performance degradation sequence" loading="lazy" /><h3>Degradation sequence</h3><p>Healthy paths render fully. Weak clients defer heavy media and motion. Runtime/API timeouts show local fallback cards and never mark work successful without proof.</p><a className="text-link" href="https://github.com/Tiancheng-Xu/agent-market/blob/main/docs/architecture/agent-market-performance-degradation-sequence.mmd">View Mermaid source</a></Panel></div><Panel><h3>Current boundary</h3><p>Implemented now: Edge SSR, hydration fallback, explicit <strong>offline</strong> health, lazy Evidence screenshots, archived AWS sample, and Preview SSR readback. TODO: automatic weak-network/low-device degraded mode, unified timeout banner, and fresh Preview Web Vitals writeback.</p></Panel></section>
    <section className="section-block"><div className="section-heading"><span className="eyebrow">LIVE AGENT RUNTIME</span><h2>Local and provider agents share one signed gateway</h2><p>The browser talks to the Cloudflare edge gateway. The gateway signs runtime requests; the local runtime calls Ollama, DeepSeek, Kimi, Qwen, or Zhipu server-side. Sequential orchestration remains available, and Queen-led GraphQL workflow now enforces task graph, ranking, accepted assignments, independent Judge, independent Final Arbiter, risk-triggered red-team, repair, and Learning Loop records. Ollama and provider keys are never exposed to the browser.</p></div><div className="evidence-gallery"><Panel className="evidence-shot"><img src="/architecture/live-agent-arbitration.svg" alt="Live agent architecture arbitration selecting hybrid option C" loading="lazy" /><h3>Architecture arbitration</h3><p>C wins: synchronous chat uses Worker, Tunnel, and local runtime; async runner control plane keeps identity, heartbeat, lease, result, and evidence.</p></Panel><Panel className="evidence-shot"><img src="/architecture/live-agent-sequence.svg" alt="Live agent request sequence through Worker, local runtime, Ollama, and provider APIs" loading="lazy" /><h3>Signed runtime sequence</h3><p>Worker validates access and signs runtime calls. Runtime verifies HMAC, then calls Ollama or provider APIs and streams SSE back.</p></Panel></div><div className="evidence-gallery"><Panel className="evidence-shot"><h3>Queen-led GraphQL workflow</h3><Badge tone="cyan">IMPLEMENTED</Badge><p><strong>/agent/graphql</strong> is the only orchestration entry for real tasks. Queen plans a DAG and may coordinate execution, but Judge and Final Arbiter must be independent agents with isolated context.</p></Panel><Panel className="evidence-shot"><h3>Risk and rescue policy</h3><Badge tone="cyan">IMPLEMENTED</Badge><p>GraphQL keeps the mandatory stages intact, applies cost-priority ranking with quality gates, auto-adds rescue policy, and triggers red-team review for high-risk or low-judge-score paths.</p></Panel><Panel className="evidence-shot"><h3>Learning Loop boundary</h3><Badge tone="cyan">IMPLEMENTED</Badge><p>Task completion writes only redacted structured summaries. Secrets, raw private prompts, local paths, model weights, and provider keys are excluded from public Evidence.</p></Panel></div><div className="evidence-gallery"><Panel className="evidence-shot"><h3>Owner-trained local runtime</h3><Badge tone="cyan">VERIFIED</Badge><p><strong>personal-ai-agent-runtime:v4.1</strong> answered through 127.0.0.1 runtime with signed request verification and Ollama thinking disabled for smoke.</p></Panel><Panel className="evidence-shot"><h3>DeepSeek API agent</h3><Badge tone="cyan">VERIFIED</Badge><p><strong>deepseek-v4-flash</strong> returned the expected smoke phrase through the provider API adapter. The API key stays in the local runtime environment.</p></Panel><Panel className="evidence-shot"><h3>Qwen API agent</h3><Badge tone="cyan">VERIFIED</Badge><p><strong>qwen-plus</strong> returned the expected smoke phrase through Alibaba Cloud Model Studio / DashScope OpenAI-compatible chat completions.</p></Panel><Panel className="evidence-shot"><h3>Zhipu API agent</h3><Badge tone="cyan">VERIFIED</Badge><p><strong>glm-5.3</strong> returned the expected smoke phrase through the Z.AI / Zhipu OpenAI-compatible chat completions endpoint.</p></Panel><Panel className="evidence-shot"><h3>Kimi API agent</h3><Badge tone="amber">PENDING CREDENTIAL</Badge><p><strong>kimi-k3</strong> is implemented and registered, but the configured Moonshot key returned provider authentication failure during smoke. It remains pending until a valid key is configured.</p></Panel></div><Panel className="diagram-panel"><a className="text-link" href="https://github.com/Tiancheng-Xu/agent-market/blob/main/docs/evidence/testing/2026-08-22-live-agent-chat-smoke.json">View sanitized live-agent smoke evidence</a><br /><a className="text-link" href="https://github.com/Tiancheng-Xu/agent-market/blob/main/docs/evidence/testing/2026-08-22-queen-graphql-runtime-smoke.json">View Queen GraphQL runtime smoke evidence</a><br /><a className="text-link" href="https://github.com/Tiancheng-Xu/agent-market/blob/main/docs/evidence/testing/2026-08-22-live-agent-validation.json">View local validation evidence</a></Panel></section>
    <section className="section-block"><div className="section-heading"><span className="eyebrow">TRACEABILITY MATRIX</span><h2>REQ-AM-01 through REQ-AM-15</h2></div><div className="requirements-list">{requirements.map((item) => <Panel key={item.requirement_id} className="requirement-row"><div className="requirement-title"><span>{item.requirement_id}</span><h3>{item.title}</h3></div><Badge tone={toneForStatus(item.status)}>{statusLabel(item.status)}</Badge><dl><div><dt>Implementation</dt><dd>{item.implementation_locations.length ? item.implementation_locations.join(", ") : "-"}</dd></div><div><dt>Tests</dt><dd>{item.test_evidence.length ? item.test_evidence.join(", ") : "-"}</dd></div><div><dt>Deployment</dt><dd>{item.deployment_evidence.length ? item.deployment_evidence.join(", ") : "-"}</dd></div><div><dt>Transaction</dt><dd>{item.transaction_evidence.length ? item.transaction_evidence.join(", ") : "-"}</dd></div><div><dt>Last verified</dt><dd>{item.last_verified_at ?? "-"}</dd></div></dl></Panel>)}</div></section>
    <section className="section-block"><div className="section-heading"><span className="eyebrow">EXTERNAL PROOF</span><h2>AWS and delivery screenshots</h2><p>Sanitized project evidence captured after external deployment and readback. Account identifiers, emails, ARNs, secrets, and unrelated resources are excluded.</p></div><div className="evidence-gallery"><Panel className="evidence-shot"><img src="/evidence/aws-cloudformation-stack.png" alt="Sanitized CloudFormation Agent Market stack view" loading="lazy" /><h3>CloudFormation stack</h3><p>Project stack in us-east-1 with CREATE_COMPLETE status.</p></Panel><Panel className="evidence-shot"><img src="/evidence/aws-runtime-exit-zero.png" alt="Sanitized AWS runtime task result" loading="lazy" /><h3>AWS runtime exit zero</h3><p>Short-lived ECS task completed with exit code 0; queues and DLQ were drained.</p></Panel><Panel className="evidence-shot"><img src="/evidence/github-actions-success.png" alt="Sanitized GitHub Actions successful run" loading="lazy" /><h3>GitHub Actions success</h3><p>Repository policy, verification, and AWS architecture checks completed successfully.</p></Panel><Panel className="evidence-shot"><img src="/evidence/cloudflare-edge-ssr.png" alt="Agent Market Evidence rendered through Cloudflare Edge SSR" loading="lazy" /><h3>Cloudflare Edge SSR</h3><p>Evidence and task routes return SSR responses; unknown routes return a real 404.</p></Panel></div></section>
    <section className="limitations"><Panel><h2>Current limitations</h2><ul><li>AWS browser-to-database performance continuity, public Evidence readback, and the reversible pause are verified.</li><li>Cloudflare Worker Edge SSR and the custom-domain cutover are verified.</li><li>No Sepolia task escrow or settlement transaction exists yet.</li><li>Stitch fixtures are design references, not project evidence.</li></ul></Panel><Panel><h2>Reproduce locally</h2><pre><code>pnpm install --frozen-lockfile{`\n`}pnpm verify{`\n`}pnpm --filter @agent-market/web build</code></pre><a className="text-link" href="https://github.com/Tiancheng-Xu/agent-market">Open repository</a></Panel></section>
  </></Localized>;
}
