# Agent Market Design System

> 本文件定义 Agent Market 的项目专属视觉语言与界面生成约束。它服务于设计师、前端工程师和 AI Agent，描述“应该呈现什么气质、为何如此、如何保持一致”。实现时仍以仓库现有组件、类型、测试、Evidence 和业务事实为准。

## 0. 设计原则

Agent Market 是一个可验证的自主协作市场，不是普通 AI 聊天页，也不是加密资产行情终端。界面必须同时表达四种品质：

1. **可信机构感**：资金、身份、权限和 Evidence 状态清晰，关键动作克制且可追溯。
2. **工程精度**：信息密度高但层级明确；状态、原因码、请求 ID 和版本号使用稳定格式。
3. **Agent 空间感**：工作流、Agent Team、Office/Cocos 和任务桌具有位置、关系与阶段感，而不是卡片的无意义堆叠。
4. **诚实边界**：`local`、`demo`、`pending`、`deferred`、`verified-production` 不得通过颜色、动画或文案互相冒充。

设计取向为：保留 Agent Market 现有 Web3/Agent 身份，吸收成熟金融产品的信任表达、工程工具的精确密度和 Office/Cocos 的空间隐喻。不得复制任何外部品牌的标志、专属组件、插画或整体页面构图。

### 核心口号

**先有证据，再报进度。**

它同时是视觉决策规则：页面先展示事实状态和下一步，再展示宣传性内容；证据不足时宁可降级，也不制造“成功感”。

---

## 1. Theme / 视觉主题与氛围

### 1.1 主题名称

**Verified Night Operations / 可验证夜间工作台**

### 1.2 氛围关键词

- 深海、夜间控制室、低眩光、可靠、精密、可审计。
- 稳定的结构线和小范围信号光，而非大面积霓虹。
- 数据密度来自明确分组，不来自缩小字号或压缩触控区域。
- Office 页面可以更具空间感，但仍属于同一控制系统。

### 1.3 视觉层级

1. **Canvas**：深海蓝黑背景，承载全局空间。
2. **Operational surface**：任务、Agent、报价、仲裁等主要操作面。
3. **Raised evidence surface**：需要关注或可展开的证据、状态和详情。
4. **Signal**：青、靛、琥珀、玫红仅用于语义，不作装饰性铺色。

### 1.4 密度模式

- 默认采用 `comfortable-dense`：桌面端可并排比较，文本仍可扫读。
- 表格、列表、工作流采用 `operational-dense`：行高不低于 `40px`，可交互行不低于 `44px`。
- 营销首页采用 `editorial-spacious`：标题与核心价值点留有更大间距，但不能变成空洞的大字海报。
- Evidence 采用 `reading-dense`：长文分节、锚点清晰、图表和台账邻近其结论。

---

## 2. Color Palette / 颜色语义

以下值以仓库现有 tokens 为基线。新实现应优先引用 CSS 变量，不在组件中散落十六进制颜色。

| Token | 基准值 | 语义角色 | 使用规则 |
| --- | --- | --- | --- |
| `--bg` | `#08101f` | 全局画布 | 页面底色，不用于卡片 |
| `--surface` | `#111b31` | 一级操作面 | 主卡片、表单、工作区 |
| `--surface-2` | `#18233b` | 二级表面 | 分组、选中前详情、嵌套区域 |
| `--surface-3` | `#202b45` | 抬升表面 | 悬停、浮层、证据摘要 |
| `--text` | `#e1e7ff` | 主文本 | 标题、关键数值、主要操作 |
| `--muted` | `#9da8c2` | 次文本 | 说明、元数据、次级标签 |
| `--line` | `rgba(181,194,232,.14)` | 结构线 | 边框、分隔线、流程连接线 |
| `--primary` | `#aaa8ff` | 主操作 | 单页唯一主 CTA、选中态 |
| `--indigo` | `#7778ff` | 链上/执行 | 已确认执行、钱包和链语义 |
| `--cyan` | `#4cdbf7` | 证据/在线 | Evidence、可验证链接、健康状态 |
| `--amber` | `#ffb454` | 风险/待确认 | 报价、到期、人工复核、不可逆提醒 |
| `--rose` | `#ff7f8e` | 失败/阻断 | 错误、离线、拒绝、危险操作 |

### 2.1 状态颜色契约

- `verified-production`：青色文字或描边，可配低透明青色底；必须有可访问 Evidence。
- `verified-local`：青色弱化版本，并明确带 `LOCAL` 文案，不能仅靠颜色区分。
- `chain verified-production`：靛蓝与青色组合；必须说明网络和回执边界。
- `pending`：中性灰蓝；若需要用户行动，再增加琥珀提示。
- `deferred`：灰蓝虚线或描边，不使用失败色。
- `demo/simulation`：琥珀标签与明确说明，不得使用“成功”图标。
- `offline/degraded/error`：玫红用于状态点或边框；大面积背景保持低透明，避免警报疲劳。
- `manual review`：琥珀，配人物/审查文案，不使用自动执行的闪电图标。

### 2.2 对比与用色限制

- 正文与背景至少达到 WCAG AA；关键小字、状态标签和表单错误不得依赖低透明度文本。
- 不允许“紫色渐变覆盖全页”的通用 AI 风格。
- 一张卡片最多存在一个主信号色；多状态用标签分组，而不是彩虹描边。
- 红色测试网横幅是全局高风险提示，应保持窄、稳定，不与页面 CTA 竞争。
- 图表不能只用红/绿区分；同时使用形状、线型、标签或纹理。

---

## 3. Typography / 字体与层级

### 3.1 字体角色

- **产品与正文**：`Space Grotesk`，回退为可用的现代无衬线字体。
- **技术与状态**：`IBM Plex Mono`，用于 eyebrow、原因码、请求 ID、版本、网络、SHA、指标名。
- 中文必须使用系统可用的高质量无衬线回退；中英文混排保持基线稳定，不为追求品牌感强制下载超大中文字体。

### 3.2 字体层级

| 层级 | 建议尺寸 | 行高 | 用途 |
| --- | --- | --- | --- |
| Display | `clamp(40px, 5vw, 68px)` | `0.98-1.05` | 首页唯一核心命题 |
| H1 | `clamp(34px, 4vw, 58px)` | `1.02-1.08` | 页面主标题 |
| H2 | `clamp(26px, 3vw, 40px)` | `1.08-1.16` | 主要模块标题 |
| H3 | `18-22px` | `1.25-1.35` | 卡片和流程段标题 |
| Body L | `17-18px` | `1.6` | 首页说明、重要解释 |
| Body | `15-16px` | `1.55-1.7` | 默认正文 |
| Meta | `12-13px` | `1.45-1.6` | 元数据、时间、辅助说明 |
| Mono label | `10-12px` | `1.3-1.5` | 状态、类型、版本、原因码 |

### 3.3 排版规则

- 标题紧凑、正文舒展。标题可使用轻微负字距，正文不得压缩字距。
- 中文标题避免在语义短语中间断行；窄屏优先缩小一级字号或限制宽度。
- 长技术标识必须可换行或横向局部滚动，不能撑破根布局。
- 纯大写仅用于短 eyebrow 和状态，不用于中文正文或长英文句子。
- 金额、评分、风险级别和链上标识必须让单位紧邻数值。

---

## 4. Component Styling / 组件与状态

### 4.1 Buttons

- 单个视觉区域最多一个 `primary` 按钮。
- `primary`：靛紫底、深色文字，表示可逆或已充分确认的主动作。
- `secondary/ghost`：透明或表面色底，用于查看、重试、返回。
- `warning`：琥珀，用于钱包签名、链上提交、资金确认、人工复核。
- `danger`：玫红，仅用于明确破坏性动作，并在动作前复述影响。
- 所有触控按钮最小高度和宽度为 `44px`；disabled 状态必须同时降低对比并保留可读原因。
- Loading 按钮保持原宽度，显示动词状态，如“正在生成报价”，不只显示旋转图标。

### 4.2 Panels and cards

- 基础面板使用 `--surface`、1px `--line` 边框和 `12-18px` 圆角。
- 业务卡片圆角保持克制；不要使用大于 `24px` 的“软糖卡片”。
- 推荐、选中和焦点状态通过边框、背景层级和小范围信号色表达，不整体放大。
- 卡片高度由内容决定。禁止让同一网格中所有卡片继承最高卡片的无意义高度。
- 卡片内部顺序：状态/类型 → 标题 → 关键事实 → 下一步 → Evidence/详情。

### 4.3 Inputs and forms

- 标签常驻，不以 placeholder 替代。
- 聚焦状态使用青色外环；错误使用玫红边框、原因和修复建议。
- 钱包地址、任务 ID、请求 ID 等按类型校验；输入框旁显示数据来源或权限边界。
- 资金表单必须同时显示预算、平台费、双方保证金、合计和退款条件。
- 不可逆提交前使用确认面板，不依赖浏览器原生 confirm。

### 4.4 Badges and status pills

- Badge 文案必须自解释：`VERIFIED LOCAL` 优于单独的绿色点。
- 同一区域最多并列三个 badge；更多状态进入 details 或证据列表。
- Badge 不模拟按钮；可点击时必须具备链接/按钮语义和焦点态。

### 4.5 Navigation

- 桌面侧栏是稳定的工作空间索引；当前路由只高亮一个项目。
- 移动端保留四个最高频入口和“更多”，不得把十个入口挤成不可读图标。
- Office 必须作为一级入口可发现，不能只藏在 Evidence 或 Agent 详情中。
- 切换路由后主内容回到该页面顶部；不得保留上一长页面的滚动 Y 位置。

### 4.6 Workflow and graph

- DAG 节点必须展示：角色、状态、输入摘要、输出摘要和失败去向。
- 边表达调用或依赖，不用作装饰。
- 可编辑图必须有未保存状态、校验错误、保存/恢复和版本信息。
- Queen、Risk Assessor、Judge、Final Arbiter 的职责要视觉隔离；Risk Assessor 不应伪装成 Queen DAG 内部节点。
- 图在窄屏可转为按顺序的阶段列表；不得用缩小到不可读的完整画布替代。

### 4.7 Office / Cocos

- Office 是任务协作状态的空间投影，不是另一个事实源。
- Cocos 仅接收脱敏快照，加载、ready、degraded、unavailable 必须有明确可见状态。
- Web fallback 与 Cocos 使用同一任务/Agent 语义和色彩，不制造两套产品。
- 角色移动或桌面选择只使用少量有意义动画；权限、钱包和执行仍在 React/服务端边界。

### 4.8 Evidence

- 每个关键声明邻近显示其状态、日期、版本和 Evidence 链接。
- Evidence 卡片区分截图、JSON 台账、链上回执、生产回读和本地测试。
- 历史证据必须标记 `superseded` 或明确版本，不删除其边界说明。
- Mermaid/运行时架构图可以嵌入，但应懒加载，具备静态回退和完整文字摘要。

---

## 5. Layout / 布局、网格与间距

### 5.1 全局尺寸

- 桌面侧栏基准：`248px`；中等桌面可收窄至约 `210px`。
- 内容最大宽度：`1440px`；主要阅读区最大宽度：`1312px`。
- 页面水平 gutter：`clamp(16px, 4vw, 64px)`。
- 顶部栏保持稳定高度，sticky 时不能遮挡锚点标题。

### 5.2 间距尺度

只使用以下节奏，避免随意的 13px、27px 等孤立值：

`4 / 8 / 12 / 16 / 24 / 32 / 48 / 64 / 96`

- 组件内部：`8-16px`
- 卡片 padding：`16-24px`
- 同组模块：`24-32px`
- 页面章节：`48-64px`
- 首页叙事章节：桌面可达 `96px`，移动端收敛到 `48-64px`

### 5.3 网格原则

- 桌面优先 12 列思想，但实现可使用 CSS Grid 的 `minmax()`。
- 同层卡片的视觉高度可以对齐，但内容区不得因最高项而产生大面积空白；操作区可通过局部 footer 对齐。
- Dashboard 先展示三项关键指标，再进入工作与查询；不得把所有状态做成同权重卡片。
- Live 页面以“工作流 → Agent 选择 → 当前对话/结果”为阅读顺序。
- Committee 页面以“事实状态 → 审查 → 唯一仲裁动作 → 规则边界”为阅读顺序。

---

## 6. Depth & Elevation / 深度与层级

### 6.1 层级系统

- `Level 0`：全局 canvas，无阴影。
- `Level 1`：常规 panel，以边框和表面差异分层。
- `Level 2`：popover、mobile-more、悬浮 inspector，使用柔和深色阴影。
- `Level 3`：阻断确认和关键错误，仅短时出现；必须有焦点管理。

### 6.2 阴影与光效

- 优先使用边框和背景层级，阴影只强调遮挡关系。
- 青色/靛蓝 glow 只用于在线核心节点、当前流程节点或 Evidence 焦点。
- 不为每张卡片添加发光，不使用玻璃拟态作为默认表面。
- `backdrop-filter` 必须有不透明背景回退，避免 SSR 首屏闪烁和低性能设备不可读。

---

## 7. Motion / 动效与反馈

- 动效只用于解释状态变化：页面进入、流程推进、Agent 匹配结果、Office readiness、错误恢复。
- 常规过渡 `160-240ms`；页面/大型阶段 `300-450ms`；不得超过 `600ms` 阻塞操作。
- 使用 opacity 与 transform，避免对 width、height、top、left 的大范围动画。
- 数据更新不让整页闪烁；只标记发生变化的数值或行。
- 所有动画在 `prefers-reduced-motion: reduce` 下关闭或降为即时淡入。
- SSR 首屏不得依赖动画初始隐藏核心内容；无 JavaScript 时仍可见、可读。

---

## 8. Responsive Behavior / 响应式规则

必须从同一组件树覆盖 `375 / 390 / 430 / 1440`，并检查至少一个中等桌面宽度。

### 8.1 Desktop

- 侧栏固定，主内容独立滚动语义清晰。
- 两列/三列仅在每列仍能满足最小内容宽度时出现。
- 表格可横向局部滚动，但页面根节点不得横向溢出。

### 8.2 Tablet / narrow desktop

- 先减少列数和 gutter，再压缩侧栏。
- 不通过整体缩放页面解决空间不足。

### 8.3 Mobile

- 侧栏切换为底部导航；页面底部为导航预留安全距离。
- 主内容单列，流程图转换为纵向阶段。
- 面板 padding 可降至 `16-17px`，正文不得低于 `14px`。
- 长地址、SHA 和 reason code 使用可换行 monospace 或局部滚动。
- 固定元素不得遮挡输入法、错误提示或最终 CTA。
- 所有触控目标至少 `44x44px`，相邻危险操作保持明显间距。

### 8.4 路由与高度

- 路由容器高度由当前页面内容决定，不使用全站最大内容高度。
- 路由切换时恢复主内容顶部，锚点导航除外。
- Cocos iframe/画布使用明确 aspect/min-height 和 fallback，不能推动其他路由继承其高度。

---

## 9. Accessibility / 可访问性

- 使用语义化 `header/nav/main/section/footer`；一页一个 H1，标题不跳级。
- 所有图标按钮有可读名称；状态点必须有文本。
- 键盘焦点使用清晰青色 focus ring，不能被 `outline: none` 移除。
- Modal、popover、mobile-more 正确管理焦点、Escape 和返回焦点。
- 表单错误通过 `aria-describedby` 关联，并在提交后聚焦第一个错误。
- 实时状态仅对关键变化使用 `aria-live`；避免连续播报动画或采样数据。
- 图表、流程图、架构图提供文字摘要；装饰图标使用空 alt。
- 视频和录屏提供标题、范围、时长、版本、字幕/文字摘要及证据边界。
- 中英文切换更新 `lang`，但专有名词、地址和代码不做错误翻译。

---

## 10. SSR & Hydration / 服务端渲染约束

- SSR 与客户端必须基于同一确定性初始状态，禁止在 render 阶段读取 `window`、随机数或本地时钟决定结构。
- 钱包、Runtime、网络和 Cocos 状态在 SSR 首屏显示诚实的未连接/待检测状态；hydration 后再渐进增强。
- 未认证、离线或 Evidence 缺失时仍渲染可理解的降级界面，不用空白骨架替代。
- 用户语言、主题和密度设置必须有稳定默认值，避免 hydration 文本错位。
- 大型 Evidence 媒体、运行时架构图和 Cocos 按路由懒加载；必须保留尺寸，避免 CLS。
- 未知路由在 SSR 和 hydration 后都保持真实 `404`，不能回退成首页 `200`。
- 可恢复 hydration 错误可以进入一次 CSR fallback，但必须记录脱敏原因码，不循环重试。

---

## 11. Content & Trust / 文案和信任表达

### 11.1 首页宣传顺序

首页不是功能清单，应按客户决策顺序表达：

1. 客户要完成什么任务。
2. 平台如何匹配与组织 Agent Team。
3. 如何通过对称保证金、风险定价和仲裁约束双方。
4. 如何用 Evidence、信誉和链上回读证明结果。
5. 当前哪些能力是生产、链上、本地、模拟或延期。

### 11.2 公平性宣传

可以强调：可审计匹配、新 Agent 探索席位、五维信誉、动态风险报价、双方确认、确定性 Gate、唯一 V3 最终仲裁者。

不得声称：

- “绝对公平”或“永不出错”。
- 本地测试等于生产能力。
- 排名模型已上线，除非有同策略生产回读。
- 模拟收益、未审计复利或未来 Yield Vault 已产生真实收益。
- 单测试钱包闭环证明多钱包角色隔离。

### 11.3 操作文案

- 使用具体动词：“生成初步报价”“确认当前 DAG”“请求钱包签名”“检查回执”。
- 不用模糊的“继续”“完成”表达不可逆步骤。
- 按钮旁说明动作发生在哪一层：浏览器、Cloudflare、AWS Runtime、Local Runtime 或 Sepolia。

---

## 12. Do / Don't

### Do

- 用状态、原因、下一步和 Evidence 构成完整反馈。
- 让风险、资金和权限在执行前可见。
- 让 Agent、任务、DAG 节点和 Office 桌位拥有稳定身份。
- 用青色表达证据和在线，用琥珀表达风险与复核，用玫红表达阻断。
- 先复用现有 tokens 和 primitives，再新增组件。
- 保持中文完整可读，英文用于专有名词和技术标识。
- 对外部依赖失败提供局部降级，不拖垮整个页面。

### Don't

- 不复制 Coinbase、Linear 或任何参考品牌的专属外观。
- 不做通用 shadcn 卡片墙、紫色渐变 Hero 或无意义玻璃拟态。
- 不让所有模块由最高卡片决定高度。
- 不在路由切换后保留上一页面滚动高度。
- 不用动画、绿色勾或“SUCCESS”掩盖 pending/local/demo。
- 不让浏览器提交服务端应推导的风险分、费率、金额或角色。
- 不在公开 UI 暴露钱包全值、Secret、私有路径、原始 Prompt 或模型权重。
- 不将 Office/Cocos 当成执行权威或 Evidence 源。

---

## 13. Agent Prompt Guide / AI Agent 实施提示

### 13.1 必须先读

实现或生成 Agent Market UI 前，AI Agent 必须：

1. 阅读本文件和目标页面现有实现。
2. 识别该页面的事实来源、状态边界和不可逆动作。
3. 优先复用现有 CSS tokens、Panel、Badge、Button、Shell 和 i18n 模式。
4. 明确 Desktop/H5 是否来自同一组件树。
5. 在视觉修改后检查 SSR、hydration、404、overflow、pageerror 和 reduced motion。

### 13.2 快速视觉提示

```text
为 Agent Market 设计一个深海蓝黑的可验证工作界面。
使用 Space Grotesk 承载产品信息，IBM Plex Mono 承载状态、证据和版本。
主表面 #111b31，结构线为低透明灰蓝；青色只用于证据与在线，
靛蓝用于已确认执行，琥珀用于风险与人工复核，玫红用于失败和阻断。
保持成熟金融产品的信任感、工程工具的精确密度和 Agent Office 的空间关系，
但不要复制外部品牌，不要生成通用 shadcn 卡片墙或紫色渐变 AI 页面。
所有状态必须同时有文本；local、demo、pending、deferred 和 verified-production 不得混淆。
```

### 13.3 页面级生成模板

```text
目标路由：[route]
用户角色：[publisher / agent owner / platform arbiter / visitor]
用户目标：[one sentence]
事实来源：[API / local runtime / evidence ledger / Sepolia readback / fixture]
可见状态：[loading / empty / ready / degraded / offline / error / manual review]
不可逆动作：[none or exact action]
Evidence 边界：[local / production / chain / historical / pending]
响应式要求：375、390、430、1440；根节点 overflow=0；触控目标 >=44px。
SSR 要求：初始结构确定，核心内容不依赖客户端动画出现，未知路由保持 404。
```

### 13.4 自检清单

- 是否一眼看懂当前状态和下一步？
- 是否把事实与宣传分开？
- 是否存在两个同权重主 CTA？
- 是否只靠颜色表达状态？
- 是否有卡片继承最大高度造成空白？
- 是否在移动端出现根级横向溢出？
- 是否尊重 reduced motion？
- SSR 与 hydration 首屏是否一致？
- 是否给图、视频和流程提供文字摘要？
- 是否误报 AWS、Sepolia、生产或收益状态？

---

## 14. 维护规则

- 设计 token 变更先更新本文件，再修改实现；语义不变的微调可在实现中完成后同步。
- 新增状态必须定义颜色、文案、可访问语义和 Evidence 边界。
- 新增核心组件必须说明 loading、empty、ready、degraded、error 和 disabled 行为。
- 视觉回归覆盖 `375 / 390 / 430 / 1440`，人工审阅差异；不得为通过 Gate 盲目更新 baseline。
- 设计评审不替代功能、类型、安全、Evidence 或性能 Gate。
- 本文件借鉴公开 DESIGN.md 方法论的章节组织，但其视觉规则全部基于 Agent Market 自身业务和现有实现。
